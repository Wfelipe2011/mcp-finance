{
  "name": "mcp-finance",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "sync": "bun run src/scripts/sync.ts",
    "enrich": "bun run src/scripts/enrich.ts",
    "digest": "bun run src/scripts/digest.ts",
    "web:dev": "bun run src/application/web/server.ts",
    "web:start": "bun run src/application/web/server.ts",
    "client:dev": "cd client && bun run dev --host",
    "client:build": "cd client && bun run build",
    "client:preview": "cd client && bun run preview"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@types/node": "^25.6.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@langchain/openai": "^1.4.5",
    "langchain": "1.3.5",
    "zod": "^4.4.3"
  }
}

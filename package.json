{
  "name": "mcp-finance",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "sync": "bun run src/scripts/sync.ts",
    "cron": "bun run src/application/cron/digest-cron.ts",
    "forecast-cron": "bun run src/application/cron/forecast-cron.ts",
    "worker": "bun run src/application/workers/enrich-worker.ts",
    "web:dev": "bun run src/application/web/server.ts",
    "web:start": "bun run src/application/web/server.ts",
    "supervisor": "bun run src/application/supervisor/supervisor.ts",
    "client:dev": "cd client && bun run dev --host",
    "client:build": "cd client && bun run build",
    "client:preview": "cd client && bun run preview",
    "mcp": "bun run src/scripts/mcp.ts",
    "mcp:dev": "bun run --watch src/scripts/mcp.ts"
  },
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0",
    "@types/bun": "^1.3.13",
    "@types/node": "^25.6.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@langchain/openai": "^1.4.5",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "bcryptjs": "^3.0.3",
    "jose": "^6.2.3",
    "langchain": "1.3.5",
    "zod": "^4.4.3"
  }
}
